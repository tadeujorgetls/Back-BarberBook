spring.application.name=baberbook

# Porta
server.port=8080

# Conexao MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/barberbook?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=tls@0528

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.validate-on-migrate=true
spring.flyway.baseline-on-migrate=true
spring.flyway.out-of-order=false

# JWT
jwt.secret=chave
jwt.expiration=3600000
app.guest-user-id=00000000-0000-0000-0000-000000000001
security.jwt.secret=dev@123
security.jwt.exp-minutes=120